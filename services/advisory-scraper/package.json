{
  "name": "bahaba-advisory-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone background Playwright scraper for MMDA and NDRRMC flood advisories saving directly to Firestore",
  "scripts": {
    "start": "tsx worker.ts",
    "dev": "tsx watch worker.ts",
    "scrape:once": "tsx worker.ts --once",
    "scrape:force": "tsx worker.ts --once --force",
    "scrape:backfill": "tsx worker.ts --once --backfill --force",
    "reparse:db": "tsx worker.ts --reparse-db"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.7",
    "firebase-admin": "^12.4.0",
    "mongodb": "^6.21.0",
    "playwright": "^1.49.1"
  }
}
